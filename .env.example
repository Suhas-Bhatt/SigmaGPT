# This file is read by docker-compose for variable substitution in docker-compose.yml.
# Backend secrets (MONGODB_URL, OPENAI_API_KEY) live in Backend/.env
# Frontend build vars live in Frontend/.env

# URL docker-compose passes to the frontend build as a build arg.
# Must match the value in Frontend/.env
VITE_API_BASE_URL=http://localhost:8080
